package com.esgi.questions;

public class UtilisateurGateway {
    public boolean isUserExisting(Long utilisateurId ){

        //To implement : when asking to UserRestService, if the user exist we receive a 200 message and we return true.
        // Else, we receive a 404 message and we return false.
        return true;
    }
}
